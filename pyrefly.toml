preset           = "strict"
project-excludes = [".venv/**", "template/**"]
project-includes = ["**/*.ipynb", "**/*.py*"]
python-version   = "3.9"
search-path      = ["."]
# winreg 仅 Windows 可用；替换为 Any 使 Linux CI 也能通过类型检查（运行时仍导入真实模块）
replace-imports-with-any = ["winreg"]
